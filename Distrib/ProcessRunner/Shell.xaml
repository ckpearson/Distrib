<metro:Window x:Class="ProcessRunner.Shell"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:metro="http://schemas.codeplex.com/elysium"
        xmlns:mpara="http://schemas.codeplex.com/elysium/params"
        xmlns:prism="http://www.codeplex.com/prism"
        mpara:Manager.Theme="Light"
        mpara:Manager.AccentBrush="{x:Static metro:AccentBrushes.Blue}"
        mpara:Manager.ContrastBrush="White"
        Title="Process Runner" Height="768" Width="1024" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        
        <ContentControl Margin="3" prism:RegionManager.RegionName="AssemblyStateRegion" Grid.Row="0" />
        <ContentControl Margin="3" prism:RegionManager.RegionName="MainRegion" Grid.Row="1" />
        <ContentControl prism:RegionManager.RegionName="BusyIndicatorRegion" Grid.Row="2" />
    </Grid>
</metro:Window>
